# Results directory

Commit experiment manifests, compact machine-readable metrics, and final report
figures. Large checkpoints are ignored. Never overwrite an existing experiment
directory; failures and partial results must remain auditable.


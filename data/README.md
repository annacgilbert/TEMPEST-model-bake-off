# Data directory

Generated arrays and trajectories are local artifacts and are ignored by Git in
`raw/` and `processed/`. Commit a manifest under `manifests/` for every dataset.
Each manifest should include provenance, configuration hashes, code revision,
units, shapes, checksums, split membership, and generation status.


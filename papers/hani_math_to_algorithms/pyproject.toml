[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hani-history-closure"
version = "0.0.1"
description = "Proof-guided cumulant and interaction-history prototypes"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
